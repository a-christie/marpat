const { Ghostbuster } = require('./ghostbuster.model');
const { Ghost } = require('./ghost.model');
const { Location } = require('./location.model');

module.exports = { Ghost, Ghostbuster, Location };
