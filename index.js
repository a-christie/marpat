'use strict';

const { connect, Client, Registry, DatabaseClient, Document, EmbeddedDocument } = require('./src');

module.exports = {
  connect,
  Client,
  Document,
  EmbeddedDocument,
  Registry,
  DatabaseClient
};
